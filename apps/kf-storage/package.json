{
  "name": "@kf/storage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Storage sweep — replicates and re-verifies artifact copies as a declared service actor",
  "main": "./dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@kf/actions": "workspace:*",
    "@kf/artifacts": "workspace:*",
    "@kf/database": "workspace:*",
    "@kf/operations": "workspace:*",
    "@kf/orchestrator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
