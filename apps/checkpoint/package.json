{
  "name": "@kf/checkpoint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Audit checkpoint signer — builds and signs Merkle roots over the audit log",
  "main": "./dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@kf/artifacts": "workspace:*",
    "@kf/canonicalization": "workspace:*",
    "@kf/database": "workspace:*",
    "@kf/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
