engine-strict=true
# Keep transitive dependencies out of the root of node_modules so a package can only
# import what it actually declares.
hoist-pattern[]=*eslint*
hoist-pattern[]=*prettier*
strict-peer-dependencies=false
