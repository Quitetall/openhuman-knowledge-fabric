# @kf/export

Builds and verifies the preservation export package (§14). This is the artifact that
survives the database: retention is unbounded, and no database binary format is readable
across that horizon.

Authority: none. The export is derived, and must be provably derivable.
