/** Compatibility surface. New modules import directly from `@kf/record-atoms`. */
export {
  createControlledObject,
  optionalString,
  requireCurrency,
  requireInteger,
  requireMinor,
  requireString,
  type NewObject,
} from '@kf/record-atoms';
