# @kf/record-atoms

Small reusable constructors and payload validators shared by domain action packages.

Authority: owns no facts. Constructors receive a transaction and create only envelopes requested
by an owning action package.
