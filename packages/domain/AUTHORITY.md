# @kf/domain

Typed domain entities and the rules that govern them. Holds no I/O: every function here
must be callable without a database, a network, or a clock passed implicitly.

Authority: none. This package describes facts; it does not own any.
